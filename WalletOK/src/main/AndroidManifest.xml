<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.theost.walletok">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Walletokandroid"
        tools:ignore="AllowBackup">
        <activity
            android:name=".presentation.entry_point.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.theost.walletok.presentation.wallet_details.WalletDetailsActivity" />
        <activity android:name=".presentation.entry_point.AuthActivity" />
        <activity
            android:name=".TransactionActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".presentation.wallets.WalletsActivity" android:theme="@style/Theme.Walletokandroid.Blue" />

    </application>

</manifest>